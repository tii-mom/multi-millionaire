/**
 * Entry point for compiled bundle. When running the compiled JS via `node dist/index.js`
 * this file will register the server. In development we run src/server.ts directly
 * using ts-node-dev.
 */
import './server';